{% extends 'base.jinja' %}

{% block content %}
  <h1>{{ name }}</h1>
  <p class="lead">
    {{ description }}
  </p>
  <div>
    <section id="builtin-man-page" class="terminal-like">
    <h3>{{ name }}</h3>
      <!-- man page content { -->
      {{ content|safe }}
      <!-- } end of man page content -->
    </section>
  </div>
{% endblock %}
