{% extends 'base.jinja' %}

{% block content %}
  <section id="builtin-man-page" class="terminal-like terminal-box">
    <h3>{{ name }}</h3>
    <!-- man page content { -->
    {{ content|safe }}
    <!-- } end of man page content -->
  </section>
{% endblock %}
